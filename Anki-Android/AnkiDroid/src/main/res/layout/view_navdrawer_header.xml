<?xml version="1.0" encoding="utf-8"?>
<!-- Small cheetah "m" mark + "SpeedyCAT" wordmark, side by side. The launcher
     FOREGROUND art (ic_launcher_foreground) is the m-cheetah on a transparent
     background, so no colored (orange/yellow) background square shows behind the
     mark; the orange/yellow fill lives in the separate ic_launcher_background.
     The foreground carries the adaptive-icon safe-zone padding, so the box is
     sized a little larger to keep the mark visually prominent. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="20dp"
    android:paddingBottom="20dp">

    <ImageView
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/ic_launcher_foreground"
        android:scaleType="fitCenter"
        android:contentDescription="@string/app_name" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="@string/app_name"
        android:textColor="?attr/navDrawerItemColor"
        android:textSize="20sp"
        android:textStyle="bold" />
</LinearLayout>
